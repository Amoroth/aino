[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
storage = { path = "../storage" }
notes = { path = "../notes" }
# maybe, the config needs it but we may yet not need the config file
serde = { version = "1.0.219", features = ["derive"] }
toml = "0.9.2"